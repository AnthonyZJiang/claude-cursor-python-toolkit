---
description: When editing tests, follow the python-tests and python-test-mocking skills.
globs: /tests/**/*.py
alwaysApply: false
---

# Python Test Rules

When writing or editing tests, read and follow:

- **python-tests** — pytest conventions, good/bad patterns, HTTP endpoint tests
- **python-test-mocking** — when mocking external boundaries